
comentário
estudos do GIT com repositório local de um projeto jágit iniciado

git init

git config user.name "Nicolly-turmaB"

git config user.email "nicolly.almeida.nicky@gmail.com"

git status

git add form1.html

git status

git commit -m "Inicio, Formulário Cadsatro Cliente HTML"

git log

git branch

git branch -m master feature-formCliente

git branch

git log

git remote add origin "link github"

git remote -v

git pull origin main

git push -u origin feature-formCliente

--------------------------------------------------------------------------

git init

git remote add origin

git remote -v 

git checkout -b feature-formCliente

git pull origin feature-formCliente

git add form1.html

git status

git commit -m "START, formulario de cadastro HTML"

git pull origin main --allow-unrelated-histories

git push origin feature-formCliente